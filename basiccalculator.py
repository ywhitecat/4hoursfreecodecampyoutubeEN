num1=input("Enter a number: ")
num2=input("Enter another number: ")

result= float(num1)+float(num2) #the int converts strings into a number variable
#but the int looks for whole number, float looks also for whole and decimals

print(result)